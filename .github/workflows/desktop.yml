name: desktop

on:
  push:
    branches: [main]
    paths:
      - 'ws-chat-desktop/**'
      - '.github/workflows/desktop.yml'
    tags: ['client-v*']
  pull_request:
    paths:
      - 'ws-chat-desktop/**'
      - '.github/workflows/desktop.yml'

permissions:
  contents: read

concurrency:
  group: desktop-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Разбор адреса и проверка понижения до http — чистая логика, ей хватает
  # обычного бегунка без Rust и без webview.
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: ws-chat-desktop/package-lock.json

      - run: npm ci
        working-directory: ws-chat-desktop

      - run: npm test
        working-directory: ws-chat-desktop

  macos:
    needs: check
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: ws-chat-desktop/package-lock.json

      # Rust на бегунке уже стоит: своя установка добавила бы минуты и ещё
      # одно стороннее действие в цепочку сборки.
      - run: rustc --version && cargo --version

      # Кэшируем только скачанные исходники зависимостей. Каталог сборки
      # весит под два гигабайта — таскать его туда-сюда дороже, чем заново
      # скомпилировать.
      - uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: cargo-${{ runner.os }}-${{ hashFiles('ws-chat-desktop/src-tauri/Cargo.lock') }}
          restore-keys: cargo-${{ runner.os }}-

      - run: npm ci
        working-directory: ws-chat-desktop

      - run: npm run build
        working-directory: ws-chat-desktop

      # Приложение не подписано: на чужой машине macOS запустит его только
      # через «Открыть» в контекстном меню. Подпись — отдельный разговор,
      # ей нужен платный сертификат разработчика.
      - uses: actions/upload-artifact@v7
        with:
          name: ws-chat-macos-aarch64
          path: ws-chat-desktop/src-tauri/target/release/bundle/dmg/*.dmg
          if-no-files-found: error

  # Отвечает ровно на один вопрос: собирается ли клиент под Windows. Как он
  # там выглядит и пускает ли микрофон, бегунок без экрана и звука не скажет,
  # поэтому сборка остаётся в прогоне и в релиз не идёт, пока её не запустит
  # живой человек.
  windows:
    needs: check
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: ws-chat-desktop/package-lock.json

      - run: rustc --version && cargo --version

      - uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: cargo-${{ runner.os }}-${{ hashFiles('ws-chat-desktop/src-tauri/Cargo.lock') }}
          restore-keys: cargo-${{ runner.os }}-

      - run: npm ci
        working-directory: ws-chat-desktop

      - run: npm run build
        working-directory: ws-chat-desktop

      - uses: actions/upload-artifact@v7
        with:
          name: ws-chat-windows-x64-untested
          path: |
            ws-chat-desktop/src-tauri/target/release/bundle/msi/*.msi
            ws-chat-desktop/src-tauri/target/release/bundle/nsis/*.exe
          if-no-files-found: error

  release:
    needs: macos
    if: startsWith(github.ref, 'refs/tags/client-v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: ws-chat-macos-aarch64
          path: dist

      - name: приложить сборку к релизу
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release create "$GITHUB_REF_NAME" dist/* --repo "$GITHUB_REPOSITORY" --title "$GITHUB_REF_NAME" --generate-notes || gh release upload "$GITHUB_REF_NAME" dist/* --repo "$GITHUB_REPOSITORY" --clobber
