{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Страница выбора сервера. Странице сервера ничего из этого не достаётся: удалённым origin разрешения не выдаются, секции remote тут нет.",
  "webviews": ["content"],
  "permissions": [
    "core:default",
    {
      "identifier": "http:default",
      "allow": [{ "url": "http://*:*/*" }, { "url": "https://*:*/*" }, { "url": "http://*/*" }, { "url": "https://*/*" }]
    }
  ]
}
